# Base URL for the backend API.
# In development, leave this as "/api" — Vite proxies /api to http://localhost:8080
# (see vite.config.js). For a separately-deployed frontend, set this to the
# backend's absolute URL, e.g. https://api.example.com/api
VITE_API_BASE_URL=/api
